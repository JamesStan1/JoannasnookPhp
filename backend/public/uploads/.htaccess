# Allow direct access to uploaded files (images, etc.)
# This overrides any parent .htaccess rewrite rules for this directory.
<IfModule mod_rewrite.c>
    RewriteEngine Off
</IfModule>

Options -Indexes
